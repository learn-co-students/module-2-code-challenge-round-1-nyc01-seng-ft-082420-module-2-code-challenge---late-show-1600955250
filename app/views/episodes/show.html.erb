<h3><%= @episode.date %></h3>
<ul>
  <% @episode.guests.each do |guest| %>
  <li><%= link_to guest.name, guest_path(guest) %></li>
  <% end %>
</ul>
<p>Average Rating for this Episode: <%= @episode.average_rating %></p>
